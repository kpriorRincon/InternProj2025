id: customModule_QPSK_Demodulator
label: QPSK Demodulator
category: '[customModule]'

templates:
  imports: from gnuradio import customModule
  make: customModule.QPSK_Demodulator()

inputs:
- label: in
  domain: stream
  dtype: complex

outputs:
- label: out
  domain: stream
  dtype: int

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
