id: customModule_QPSK_Modulator
label: QPSK Modulator
category: '[customModule]'

templates:
  imports: from gnuradio import customModule
  make: customModule.QPSK_Modulator()

inputs:
- label: in
  domain: stream
  dtype: int

outputs:
- label: out
  domain: stream
  dtype: complex

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
